#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int N;
	scanf("%d", &N);
	int arr[50];
	for (int i = 0; i < N; i++)
	{
		scanf("%d", &arr[i]);
	}
	int del;
	scanf("%d", &del);
	//遍历数组
	for (int i = 0; i < N; i++)
	{
		//如果等于要删的数continue
		if (arr[i] == del)
		{
			continue;
		}
		//不等于打印
		else
		{
			printf("%d ", arr[i]);
		}
	}
	printf("\n");
	return 0;
}
