#include<stdio.h>
#include<string.h>
int main()
{
    char a[100],b[100];
    char name[] = "admin";
    char password[] = "admin";
    while(scanf("%s %s",&a,&b) != EOF)
    {
        if(strcmp(a,name) == 0 && strcmp(b,password) == 0)
        {
            printf("Login Success!\n");
        }
        else
        {
            printf("Login Fail!\n");
        }
    }
    return 0;
}
