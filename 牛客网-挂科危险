#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    if((n >= 10) && (n <=30))
    {
        printf("Danger++\n");
    }
    else if((n >= 4) && (n <= 9))
    {
        printf("Danger\n");
    }
    else if((n >= 0) && (n <= 3))
    {
        printf("Good\n");
    }
    return 0;
}
