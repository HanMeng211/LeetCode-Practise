#include<stdio.h>
int main()
{
    float arr[5];
    float sum = 0.00;
    for(int i = 0;i < 5;i++)
    {
        scanf("%f",&arr[i]);
        sum = sum + arr[i];
    }
    printf("%.2f\n",sum / 5);
    return 0;
}
